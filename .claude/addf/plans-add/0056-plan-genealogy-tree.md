# Plan 0056: Plan 系統樹 — 剪定・派生・復活を一級データにするロードマップ表現

## 実装状況: 進行中（2026-07-17 標準テンプレートへ昇格・着手。オーナー5問回答済み）

owner_feedback: 済
edge: absorbed-into 0067

## 関連 Plan

- [Plan 0067: Plan ブレインストーミングのスキル化](0067-plan-brainstorm-skill.md)（検討スタブ） — 本 Plan の未解決の問い「剪定メタデータの必須範囲」対話中にオーナーから示された「Plan ブレインストーミングのスキル化」構想を独立 Plan として切り出し

## エッジ行の書式（確定仕様）

Plan の既存慣習（`## 実装状況:` ヘッダ直後の行頭 key:value — `owner_feedback` /
`execution_style` と同型・grep 行頭一致で拾える）に合わせ、YAML frontmatter は導入しない:

```
edge: derived-from 0053
edge: absorbed-into 0043
edge: pruned
edge: revives 0026
edge: blocked-by owner
edge: blocked-by external gh-pages-setting
edge: blocked-by 0040
```

- 1エッジ1行・複数可。値は `<型> <対象>`。対象は Plan 4桁番号、blocked-by のみ
  `owner` / `external <一言>` / Plan 番号を許す（オーナー回答: オーナーブロックと
  それ以外のブロックを分けて表示）
- **pruned は自己マーカー（target を取らない — 本 Plan 自体が剪定されたことを表す。案レベルの剪定は「関連 Plan」節の記録のみ）でメタデータ必須**（オーナー回答: pruned のみ必須・他は任意）。詳細は
  「関連 Plan」セクション側に `理由: / 証拠: / 復活条件:` を書く（1行に収まらないため）。
  lint は pruned エッジを持つ Plan の「関連 Plan」節に3項目の存在を要求する
- **併用の同期**（オーナー回答: frontmatter 相当＋セクションの併用）: `edge:` 行に Plan 番号を
  持つエッジは、「関連 Plan」セクションに対応するリンクが存在すること（lint 検査）。
  逆方向リンク（knowhow 双方向原則）は既存の「関連 Plan は双方向」宣言に従う
- 完了 Plan にも edge 行は残す（来歴 — owner_feedback と違い完了後も削除しない）

## 変更内容（フェーズ）

### フェーズ1: エッジ基盤と系統樹ビュー

- `PlanTemplate.md` に edge 行の書式コメントを追記
- `generate-dashboard.py` に「系統樹」ページを追加:
  - 全 Plan の `edge:` 行と TODO 状態をパースし Mermaid `graph TD` を生成
  - ノード装飾: 完了=灰 / 進行中=青 / 未着手=白 / **blocked-by owner=赤枠強調** /
    blocked-by external・Plan=黄枠（オーナー/その他ブロックの区分表示）
  - pruned エッジは点線・revives は太線で「探索の地形」を可視化
  - Mermaid 描画: `mermaid` を devDependency に追加し、ダッシュボードのテーマで
    クライアントサイドレンダリング（ローカル専用のため CDN 不使用）
- `lint-template-sync.py` にペア9(edge 整合)ではなく**独立 lint `lint-genealogy.py`** を新設
  （edge 構文・対象 Plan 実在・pruned メタデータ・セクション併記の検査。
  lint-template-sync のペア追加は addf-lint 表更新義務が重いため独立ファイルにする）
- テスト: 合成 Plan フィクスチャ（drift-injection・Plan 0件 DS でも成立）

### フェーズ2: 全件遡及付与

- Plan 0001〜0068 の全件に edge 行を遡及付与（オーナー回答: 動作確認を兼ねて全件）
- 実例系譜（0026/0043/0044/0045/0049/0053/0054 等）は Plan 本文・出典欄から根拠を取る。
  エッジが1本も無い Plan は付与しない（無理に作らない — 孤立ノードも情報）
- 付与後に lint-genealogy・run-all 全通過を確認

## 影響範囲

- generate-dashboard.py・PlanTemplate.md・package.json（mermaid devDependency）・
  addfTools（lint-genealogy.py 新設）・tests・plans-add 全件（edge 行のみ・本文不変）
- lint-plan-status.py は `## 実装状況:` 行のみ検査のため干渉しない

## テスト方針

- lint-genealogy: 合成フィクスチャで構文エラー・存在しない対象・pruned メタデータ欠落・
  セクション不併記を検出（欠如=SKIP 設計・DS で誤 FAIL しない）
- ダッシュボード: 系統樹ページの生成・Mermaid コード内に owner ブロックの classDef が
  含まれることを grep 検証。描画はブラウザ目視 <!-- human-judgment -->

## 破壊的変更の許容範囲

なし（edge 行の追加のみ。既存パーサ（owner_feedback 等）は行頭一致で無干渉）

## 完了条件

- [ ] `edge:` 行書式が PlanTemplate.md に記載されている
- [ ] ダッシュボードに系統樹ページが生成され、owner/external ブロックが区分表示される
- [ ] lint-genealogy.py が構文・実在・pruned メタデータ・併記を検査し run-all に組み込まれている
- [ ] 全 Plan（0001〜0068）に実在系譜の edge 行が遡及付与され lint 通過
- [ ] ブラウザで系統樹を目視確認（オーナー） <!-- human-judgment -->

> 出典: オーナー発案（2026-07-16 対話セッション）。未完了タスクロードマップを「純粋な
> フェーズ進行（積み上げ式）」から「クリティカルパスを意識した系統樹」で表す構想。
> 剪定された計画・不採用計画にも系譜があってよく、後の計画で同じ着想が復活したときに
> 「もう考えたな」と辿れる状態を作る。

## 分かっていること

- **系統樹はデータとして既に半分実在する**。線形テーブル（TODO.addf.md）の裏で、Plan 間の
  関係は既に系譜的な語彙で記録されている:
  - 派生（切り出し）: Plan 0053 のレビュー → Plan 0054 起票（Progress 運用ルール7
    「主題外は別 Plan に切り出す」は系統樹の分岐を生むルールそのもの）
  - 回収（吸収）: Plan 0026 の残り → Plan 0043 に回収
  - 剪定: Plan 0045 は実測（EnumaElish・wasurenainder）に基づく「意図的な不採用として決定」。
    Plan 0044 も案A採用＝案Bを証拠付きで剪定
  - 復活: Plan 0054 の出典欄が実例 — Plan 0026 で検出 → 0043 で明示的にスコープ外（剪定）→
    放置 → 0053 の doc-review で発掘 → 0054 として復活
  - 投機: Plan 0049 は worktree で投機実装 → 後日 main へ回収・採番変更
- `PlanTemplate.md` には「関連 Plan」セクションが既にあり、knowhow と同じ双方向リンク原則を
  宣言済み。欠けているのは (a) エッジの型分類、(b) グラフとしての描画、の2つ
- **エッジ型の最小セット案**（実績のある関係のみ。型を増やしすぎると記入コストと lint
  ノイズが増える — B2 保留時の「悩みどころ・ノイズは増えてほしくない」というオーナー感性）:

  | エッジ型 | 意味 | 実例 |
  |---|---|---|
  | `derived-from` | レビュー・実装中の発見から切り出された | 0053 → 0054 |
  | `absorbed-into` | 残タスクが別 Plan に回収された | 0026 → 0043 |
  | `pruned` | 証拠付きで不採用（理由と証拠を必須にする） | 0045（実測） |
  | `revives` | 剪定済みの枝を再起票 | 0054 revives 0026-Critical |
  | `blocked-by` | 依存・オーナー判断待ち | 0040 フェーズ4 ← フェーズ3 |

- **剪定枝は「死んだノード」ではなく「復活トリガー付きの休眠ノード」**。ADDF には既に
  「実害の実測をトリガーにする」判断様式が繰り返し現れている（B2 保留・Plan 0043 の
  事後観測方式・Plan 0054 の着手トリガー）。剪定ノードに `pruned(理由, 証拠, 復活条件)` を
  持たせると、復活検討時に問うべきことが「前提が変わったか？」の一点に圧縮される。
  代替わり日記が「文脈」、チェックボックスが「事実」を残すのに対し、系統樹は
  「決定の地形」を残す第三の層。不採用計画が「失敗の記録」ではなく「探索した証拠」として
  一級市民になるのは Plan 0016〜0020 の趣旨（責めない）とも整合する
- **このリポジトリでのクリティカルパスの実態は「オーナーの決断キュー」**。Plan 間の技術的
  依存は薄く、真のブロッカーはオーナー判断待ち（0048=Q6・0030 branch protection・
  0039 フェーズ3・0054 方向性選択）。系統樹の葉のうち `blocked-by: owner` の枝を強調する
  だけで、Questions.md と TODO 状態注記に散らばる「待ち」が一望できる。Dashboard.md
  （unattended 差分まとめ）とも相性が良い
- **実装方針の骨子**（詰めは標準テンプレート昇格時）:
  - TODO テーブルは置き換えない。線形テーブルは lint（lint-plan-status.py）と状態管理の
    真実源として維持し、系統樹は Plan の「関連 Plan」セクション＋メタデータから**生成する
    派生ビュー**にする（CLAUDE.repo.example.md の単一ソース化の教訓に従う。手書きの
    系統樹図は即ドリフトする）
  - 描画は Mermaid `graph` で十分（VitePress サイト＝Plan 0039 に載せられる）
  - 整合性検査は knowhow 双方向リンク lint（セクション8）と同型のため設計資産を流用できる。
    既知の罠も同じものを踏む想定（`## 実装状況:` ヘッダの1行制約・エッジ追記が自分の新設
    lint に引っかかる Plan 0052 型の自己言及問題）
- **発展の面白い可能性**: 新 Plan 起票時に addf-knowhow-filter と対になる「系譜フィルタ」—
  起票しようとしている内容が過去の剪定枝と類似していないかを照合し、「その枝は剪定済み。
  復活条件はこれ」と返す（「もう考えたな」の自動化）。/addf-plan-audit が既に行っている
  「埋没発掘」の常設ビュー化とも言える

## 未解決の問い → オーナー回答（2026-07-16 ダッシュボードのアンカーコメント経由）

- エッジ型の最小セットを上記5種で確定してよいか
  - **回答（dc_mrnmcpyc51gr）**: 暫定的にこれでよい（5種で開始・運用で見直し）
- 剪定メタデータ（理由・証拠・復活条件）をどこまで必須にするか
  - **回答（dc_mrnmgc32vjlq）**: おまかせ → **エージェント推しで確定: `pruned` エッジのみ
    必須・他は任意**。あわせて設計の狙いとして「『Plan 100 まで ADDF を良くするよう
    何ターンかけて考えてもいいよ』とプロンプトしたときに、ノードを広げる・深める・
    つなげる動きがイメージできる系統樹であること」が示された。**Plan ブレインストーミングの
    スキル化構想**（AI 起票案は人間の採否判断が要るため既存 Plan と領域を分ける）は
    Plan 0067 として切り出し済み
- エッジの記録先
  - **回答（dc_mrnmgq3qak3d）**: **併用**（frontmatter = 機械可読 ＋「関連 Plan」
    セクション = 人間可読。lint で両者の同期を検査する前提）
- クリティカルパス表示を初期スコープに含めるか
  - **回答（dc_mrnmhs1gliki）**: 初期スコープに含める方向で、**「オーナーブロック」と
    「それ以外の理由によるブロック」を分けて表示する**（blocked-by: owner /
    blocked-by: external 等の区分）
- 過去 Plan への遡及付与の範囲
  - **回答（dc_mrnmigin4pys）**: **動作確認も兼ねて全件遡及する**

## 着手のトリガー

- ~~オーナーが「未解決の問い」の方向性（少なくともエッジ記録先と初期スコープ）を
  選択したとき、標準テンプレートに書き直して着手する~~ →
  **全ての問いに回答済み（2026-07-16）: 5種暫定 × pruned のみ必須 × 併用記録 ×
  クリティカルパス初期スコープ入り（オーナー/その他ブロック区分表示） × 全件遡及**。
  標準テンプレートへの書き直しから着手できる
